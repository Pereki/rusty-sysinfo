name: Cargo test

on:
  pull_request:
    branches: ["dev", "main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Build project
        run: cargo build --verbose 
 
      - name: Run tests
        run: cargo test --verbose
